# src/test/resources/application-test.properties
aws.region=us-east-1
database.url=jdbc:postgresql://localhost:5432/testdb
database.writer=postgres_admin
database.password=postgres_admin_password
spring.datasource.url=${database.url}
spring.datasource.username=${database.writer}
spring.datasource.password=${database.password}
spring.datasource.driver-class-name=org.postgresql.Driver
data.warehouse.bucket=data-warehouse
aws.amtk.replication.bucket=ptc-p-logs
enable.data.warehouse=true
enable.replication=true
!aws.endpoint.override=http://localhost:4566